 pipeline {
     agent any
     stages {
            stage("Get code github from MAIN") {
               steps {
                echo 'gitlab MAIN'
               }
            }
            // stage("Build Dockerfile"){
            //     steps{
                    
            //     }
            // }
        }
    }